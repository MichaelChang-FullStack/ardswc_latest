
CREATE TABLE class.dbo.ClassDataIndoor (
    Id INT IDENTITY(1,1) PRIMARY KEY,
    Country VARCHAR(255),
    SchoolName VARCHAR(255),
    [Session] VARCHAR(255),
    [Attribute] VARCHAR(255),
    BaseType VARCHAR(255),
    Area VARCHAR(255),
    URL VARCHAR(255),
    Category VARCHAR(255),
    Class_Map TEXT,
    Add_Date DATETIME DEFAULT CURRENT_TIMESTAMP,
    Add_By INT DEFAULT -1,
    Edit_Date DATETIME DEFAULT CURRENT_TIMESTAMP,
    Edit_By INT DEFAULT -1,
    Del_Flag INT DEFAULT 0
);

insert into class.dbo.ClassDataIndoor (Country) values('Test')


SELECT Id,Country,SchoolName,[Attribute] AS Attribute ,BaseType ,URL,A.Area,Category,Class_Map FROM dbo.ClassDataIndoor WHERE Class_Map IS NOT NULL AND DATALENGTH(Class_Map) > 0 AND Del_Flag=0 ORDER BY Id;


SELECT A.Area ,COUNT(A.Area)  FROM class.dbo.ClassDataIndoor A WHERE A.Class_Map IS NOT NULL GROUP BY A.Area ;


SELECT *  FROM class.dbo.ClassDataIndoor A;

/*中部地區
北部地區
東部地區
南部地區
離島地區*/

SELECT A.Id, A.Country, A.SchoolName, A.Class_Map
FROM class.dbo.ClassDataIndoor A
WHERE A.Area = N'離島地區'
ORDER BY A.Id;

DROP TABLE class.dbo.ClassDataIndoor ;
